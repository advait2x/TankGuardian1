/**
 * Feature flags and configuration
 */

// Toggle to enable/disable remote catalog fetching from Supabase
// Set to false to always use mock data (useful for development/testing)
export const USE_REMOTE_CATALOG = true;

